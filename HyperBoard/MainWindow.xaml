<metro:MetroWindow x:Class="HyperBoard.MainWindow"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:lc="clr-namespace:HyperBoard"
                   xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                   Title="MainWindow" Width="525"
                   Height="350" EnableDWMDropShadow="True">
    <metro:MetroWindow.DataContext>
        <lc:MainWindowViewModel />
    </metro:MetroWindow.DataContext>
    <metro:MetroWindow.RightWindowCommands>
        <metro:WindowCommands>
            <Button Content="Fly" />
        </metro:WindowCommands>
    </metro:MetroWindow.RightWindowCommands>
    <Canvas>
        <Ellipse Canvas.Left="{Binding Position[0]}"
                 Canvas.Top="{Binding Position[1]}"
                 Width="50" Height="50"
                 Fill="Black" />
    </Canvas>
</metro:MetroWindow>
